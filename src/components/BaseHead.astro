---
interface Props {
  title: string;
  description: string;
  keywords?: string | string[];
  image?: string;
  canonicalURL?: URL | string;
  type?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  noindex?: boolean;
}

const {
  title,
  description,
  keywords,
  image = '/images/og-banner.webp',
  canonicalURL = Astro.url,
  type = 'website',
  publishedTime,
  modifiedTime,
  noindex = false,
} = Astro.props;

const defaultKeywords = "abonnement iptv, iptv abonnement, iptv premium, code iptv, abonnement iptv 12 mois, iptv france, meilleur iptv, iptv sans coupure, streaming 4k, iptv smart tv, cle tv 4k iptv";
const keywordsString = Array.isArray(keywords) 
  ? keywords.join(', ') 
  : (keywords || defaultKeywords);

const siteUrl = 'https://abonnement-iptv.cloud';
const fullCanonicalURL = typeof canonicalURL === 'string' ? canonicalURL : new URL(Astro.url.pathname, Astro.site || siteUrl).toString();
const ogImageUrl = new URL(image, Astro.site || siteUrl).toString();
---

<!-- Global Metadata -->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0" />
<meta name="generator" content="Astro v5.3" />
<meta name="theme-color" content="#090A10" />
<meta name="color-scheme" content="dark" />

<!-- SEO Canonical & Hreflang Tags -->
<link rel="canonical" href={fullCanonicalURL} />
<link rel="alternate" hreflang="fr-FR" href={fullCanonicalURL} />
<link rel="alternate" hreflang="fr" href={fullCanonicalURL} />
<link rel="alternate" hreflang="x-default" href={siteUrl} />

<!-- Custom Favicon & Device Icons (No default Astro icons) -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="apple-touch-icon" href="/favicon.svg" />
<meta name="apple-mobile-web-app-title" content="Abonnement IPTV" />
<meta name="application-name" content="Abonnement IPTV France" />

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<meta name="keywords" content={keywordsString} />
<meta name="author" content="Abonnement IPTV France" />
<meta name="publisher" content="Abonnement IPTV France" />
{noindex ? (
  <meta name="robots" content="noindex, nofollow" />
) : (
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
)}

<!-- Open Graph / Facebook -->
<meta property="og:type" content={type} />
<meta property="og:url" content={fullCanonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImageUrl} />
<meta property="og:site_name" content="Abonnement IPTV France" />
<meta property="og:locale" content="fr_FR" />
{publishedTime && <meta property="article:published_time" content={publishedTime} />}
{modifiedTime && <meta property="article:modified_time" content={modifiedTime} />}

<!-- Twitter / X -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={fullCanonicalURL} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={ogImageUrl} />

<!-- Performance Preconnect -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
